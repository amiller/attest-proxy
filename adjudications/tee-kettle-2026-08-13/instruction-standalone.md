Two AMD product-line questions. Answer them as hardware catalog facts.

A public post references the MMIO address 0xf80c2094 and a population of roughly
100 million AMD CPUs.

(1) An MMIO address in that region belongs to which AMD platform block, and
therefore what would a CPU population characterised that way consist of —
client/consumer parts (Ryzen, APUs, their chipsets), EPYC server parts, or both?

(2) Which AMD product lines have ever implemented SEV-SNP, and from which Zen
generation onward?

(3) Do those two sets overlap? Specifically: has any CPU family belonging to the
population in (1) ever shipped SEV-SNP support?

Under 200 words. If the address region alone is not enough to identify the
platform, say so plainly rather than guessing.
