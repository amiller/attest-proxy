Two questions about AMD product segmentation and platform architecture. Both are
general hardware facts about which product class contains which silicon — not
about any security topic.

1. Which AMD product lines implement SEV-SNP, and from which Zen generation
   onward? State explicitly whether SEV-SNP has ever shipped on consumer Ryzen
   desktop, mobile, or APU parts.

2. The AMD Fusion Controller Hub (FCH) and its fixed platform-configuration
   registers: is the FCH a component of AMD's client platform (desktop, mobile,
   APU) or of the EPYC server platform? And separately, an installed base on the
   order of 100 million units — does that scale correspond to AMD's consumer
   client segment or to its EPYC server segment?

Answer both from general knowledge. Under 160 words.
